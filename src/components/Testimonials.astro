---
const testimonials = [
  {
    name: 'María García',
    role: 'CEO, TechStore',
    text: 'Orquest transformó nuestra atención al cliente. El chatbot resuelve el 80% de las consultas automáticamente. Nuestro equipo ahora se enfoca en ventas.',
    avatar: 'MG',
  },
  {
    name: 'Carlos López',
    role: 'Director de Operaciones, LogiPro',
    text: 'La automatización de nuestros flujos de trabajo nos ahorró 40 horas semanales. El ROI fue visible desde el primer mes.',
    avatar: 'CL',
  },
  {
    name: 'Ana Rodríguez',
    role: 'Fundadora, EduOnline',
    text: 'Implementaron un sistema de onboarding automático que mejoró nuestra retención de alumnos en un 60%. Increíble equipo.',
    avatar: 'AR',
  },
];

const techs = ['OpenAI', 'n8n', 'Python', 'Node.js', 'WhatsApp API', 'Telegram', 'Make', 'PostgreSQL'];
---

<section class="relative section-padding">
  <div class="max-w-7xl mx-auto">
    <!-- Testimonials -->
    <div class="text-center mb-16">
      <span class="reveal inline-block px-4 py-1.5 rounded-full text-xs font-semibold uppercase tracking-widest text-primary-light bg-primary/10 border border-primary/20 mb-6">
        Testimonios
      </span>
      <h2 class="reveal font-display font-bold text-3xl sm:text-4xl md:text-5xl text-white mb-6">
        Lo que dicen nuestros <span class="gradient-text">clientes</span>
      </h2>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-24">
      {testimonials.map((t, i) => (
        <div class={`reveal reveal-delay-${i + 1} glass-card glow-border p-8 hover:bg-dark-600/60 transition-all duration-500`}>
          <!-- Stars -->
          <div class="flex gap-1 mb-4">
            {[...Array(5)].map(() => (
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
              </svg>
            ))}
          </div>

          <p class="text-slate-300 leading-relaxed mb-6 italic">"{t.text}"</p>

          <div class="flex items-center gap-3 pt-4 border-t border-white/5">
            <div class="w-10 h-10 rounded-full bg-gradient-to-br from-primary to-accent flex items-center justify-center text-sm font-bold">
              {t.avatar}
            </div>
            <div>
              <p class="font-semibold text-white text-sm">{t.name}</p>
              <p class="text-xs text-slate-500">{t.role}</p>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Tech stack -->
    <div class="reveal text-center">
      <p class="text-sm text-slate-500 uppercase tracking-widest mb-8">Tecnologías que utilizamos</p>
      <div class="flex flex-wrap justify-center gap-4">
        {techs.map(tech => (
          <div class="px-6 py-3 glass-card text-sm font-medium text-slate-300 hover:text-white hover:border-primary/30 transition-all duration-300 cursor-default">
            {tech}
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
